import { RtClickDirective } from './rt-click.directive';

describe('RtClickDirective', () => {
  it('should create an instance', () => {
    const directive = new RtClickDirective();
    expect(directive).toBeTruthy();
  });
});
