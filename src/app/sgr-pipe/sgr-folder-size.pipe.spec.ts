import { SgrFolderSizePipe } from './sgr-folder-size.pipe';

describe('SgrFolderSizePipe', () => {
  it('create an instance', () => {
    const pipe = new SgrFolderSizePipe();
    expect(pipe).toBeTruthy();
  });
});
